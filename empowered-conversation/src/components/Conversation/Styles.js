import styled from "styled-components";
import { background, primary, responsiveWidth } from "../styled/reusable";

export const Wrapper = styled.div`
  ${background};
  width: 100%;
  height: 100%;
  margin: 0 auto;
  display: flex;
  justify-content: center;
  align-items: center;
`;

